#include "libftprintf.h"
#include <stdio.h>

int	main()
{
	// int		i;
	// void	*ptr;
	// int		a;
	// int		*p;

	// i = -2147483648;
	// ptr = (void *)0x12345678;
	// a = 5;
	// p = &a;
	// ft_printf("i code %s and i want to be %c good coder in %d year", "very well", 'a', i);
	// ft_printf("Ahmad got 100%% in this project");
	// ft_printf("pointer Adress : %p", ptr);
	// ft_printf("%s", (char *)0);
	// ft_printf("%d, %u, %p", p, p, p);
	// ft_printf("%X", -10);
	printf("%X", -10);
	// printf("%d", i);
	return (0);
}
